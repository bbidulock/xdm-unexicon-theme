xdm-unexicon-theme -- history of user-visible changes.  2019-09-08

Release xdm-unexicon-theme-1.4 released 2019-09-08
--------------------------------------------------

git tag -n100 -l 1.4

1.4             Maintenance Release 1.4

    This is the fourth release of the xdm-unexicon-theme package. This
    package contains and XDM theme for unexicon.

    This release is a maintenance release whose sole purpose is to update
    the build package, generate a better NEWS file, handle annotated tags
    better, and converts the release archives to lzip compression.

    Includes in the release is an autoconf tarball for building the release
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.3...1.4

Brian Bidulock <bidulock@openss7.org> (10):
      reformat TODO
      update for gettext 0.20.1
      build better NEWS file
      build better release artifacts
      update release notes
      place filename last in sed command
      build release notes with dist
      have better defaults and no c compiler
      ignore cscope output
      ignore more products


Release xdm-unexicon-theme-1.3 released 2018-10-01
--------------------------------------------------

git tag -n100 -l 1.3

1.3             [xdm-unexicon-theme -- release notes.  2018-01-06]: #

    Maintenance Release 1.3
    =======================

    This is the second release of the xdm-unexicon-theme package.  This
    package contains and XDM theme for unexicon.

    This release is a maintenance release and fixes some issues with the
    NumLock begin on when the login window appears.  This release turns
    NumLock off before launching the login window.

    Includes in the release is an autoconf tarball for building the release
    from source.  See the [NEWS](NEWS) and [TODO](TODO) file in the release
    for more information.

    [ vim: set ft=markdown sw=4 tw=72 nocin nosi fo+=tcqlorn spell: ]: #

git shortlog -e -n -w80,6,8 1.2...1.3

Brian Bidulock <bidulock@openss7.org> (4):
      turn off numlock before launching slim
      update release notes
      update release files
      update release files


Release xdm-unexicon-theme-1.2 released 2018-09-30
--------------------------------------------------

git tag -n100 -l 1.2

1.2             [xdm-unexicon-theme -- release notes.  2018-01-06]: #

    Maintenance Release 1.2
    =======================

    This is the second release of the xdm-unexicon-theme package.  This
    package contains and XDM theme for unexicon.

    This release is a maintenance release and fixes some issues with the
    NumLock begin on when the login window appears.  This release turns
    NumLock off before launching the login window.

    Starting with this release, commits are verified and tags are signed and
    annotated (with this text).

    Includes in the release is an Arch Linux source tarball (also on the
    AUR) and an autoconf tarball for building the release from source.
    See the [NEWS](NEWS) and [TODO](TODO) file in the release for more
    information.

    [ vim: set ft=markdown sw=4 tw=72 nocin nosi fo+=tcqlorn spell: ]: #

git shortlog -e -n -w80,6,8 1.1...1.2

Brian Bidulock <bidulock@openss7.org> (13):
      update ignored files
      good build
      passes distcheck
      added ignore file
      use multicast addresses too
      change acess and chooser selection for xde-session
      set vendor id in chooser, add maintainer scripts
      set seat number on local displays
      add missing files
      remove release files from ignore
      update build system
      release update
      update release files


Release xdm-unexicon-theme-1.1 released 2014-03-27
--------------------------------------------------

git shortlog -e -n -w80,6,8 1.1

Brian Bidulock <bidulock@openss7.org> (1):
      initial commit

